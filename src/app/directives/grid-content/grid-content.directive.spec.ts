import { GridContentDirective } from './grid-content.directive';

describe('GridContentDirective', () => {
  it('should create an instance', () => {
    const directive = new GridContentDirective();
    expect(directive).toBeTruthy();
  });
});
